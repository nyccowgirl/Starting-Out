/*
 
 10.1   isalpha - returns true if letter of the alphabet
        isalnum - returns true if letter of the alphabet or digit of 0-9
        isdigit - returns true if digit 0-9
        islower - returns true if lower case letter
        isprint - returns true if printable character, including space
        ispunct - returns true if printable character, other than letter, digit or space
        isupper - returns true if upper case letter
        isspace - returns true if whitespace character such as space, new line, vertical/tab
        toupper - returns upper case equivalent
        tolower - returns lower case equivalent
 
 10.2   little = tolower(big);
 
 10.3   if (isdigit(ch)) {
            cout << "digit";
        } else {
            cout << "Not a digit";
        }
 
 10.4   A
 
 10.5   do {
            cout << "Do you want to repeat the program or quit? (R/Q)";
            cin >> input;
        } while (toupper(input) != 'R' && toupper(input) != 'Q');
 
 10.6   strlen - returns length of c-string, not including null terminator
        strcat - appends 2nd string to 1st string
        strcpy - copies 2nd string to 1st string
        strncat - appends 2nd string up to specified max to 1st string
        strncpy - copes 2ns string up to max to 1st string
        strcmp - compares if 2 strings are the same and/or returns which is alphabetically >
        strstr - searches for occurrence of 2nd string in 1st string
 
 10.7   4
 
 10.8   Have a nice day
        nice day
 
 10.9   strcpy(composer, "Beethoven");
 
 10.11  atoi - converts string to integer
        atol - converts string to long integer
        atof - converts string to double
        itoa - converts integer to string into specified character array with specified base
 
 10.12  num = atoi("10");

 10.13  num = atol("100000");
 
 10.14  num = atof("7.2389");
 
 10.15  itoa(127, value, 10);
 
 */
