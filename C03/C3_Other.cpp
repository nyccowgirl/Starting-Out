/*
 
 3.1    #include <iostream>
 
 3.2    true
 
 3.3    B
 
 3.4    Enter miles, feet and inches: 2345 18 4.5
        cin >> miles >> feet >> inches
 
 3.7    6 + 3 * 5 = 21
        12 / 2 - 4 = 2
        9 + 14 * 2 - 6 = 31
        5 + 19 % 3 - 1 = 5
        (6 + 2) * 3 = 24
        14 / (11 - 4) = 2
        9 + 12 * (8 - 3) = 69
        (6 + 17) % 2 - 1 = 0
        (9 - 3) * (6 + 9) / 3 = 30
 
 3.8    y = 6 * x
        a = (2 * b) + (4 * c)
        y = pow (x, 2.0)
        g = (x + 2) / pow(z, 2.0)
        y = pow(x, 2.0) / pow(z, 2.0)
 
 3.11   b / a = 2
        x * a = 17
        static_cast<double>(b / a) = 2
        static_cast<double>(b) / a = 2.4
        b / static_cast<double>(a) = 2.4
        static_cast<double>(b) / static_cast<double>(a) = 2.4
        b / static_cast<int>(x) = 4
        static_cast<int>(x) * static_cast<int>(z) = 27
        static_cast<int>(x * z) = 30
        static_cast<double>(static_cast<int>(x) * static_cast<int>(z)) = 81
 
 3.14   total = subtotal = tax = shipping = 0;
 
 3.15   x += 6
        amount -= 4
        y *= 4
        total /= 27
        x %= 7
        x += y * 5
        total -= discount * 4
        increase *= salesRep * 5
        profit /= shares - 1000
 
 3.20   cos - cosine
        log - natural logarithm
        sin - sine
        exp - exponent
        log10 - base-10 logarithm
        sqrt - square root
        fmod - remainder first arguument divided by second one
        pow - power of
        tan - tangent
 
 3.21   x = sin(angle1) + cos(angle2)
 
 3.22   y = pow(x, (1 / 5))
 
 3.23   y = 1 / (sin(a))
 
 */
